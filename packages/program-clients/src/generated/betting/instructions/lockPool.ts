/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_BETTING_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const LOCK_POOL_DISCRIMINATOR = new Uint8Array([
  154, 202, 217, 175, 178, 161, 30, 152,
]);

export function getLockPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LOCK_POOL_DISCRIMINATOR);
}

export type LockPoolInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      ...TRemainingAccounts,
    ]
  >;

export type LockPoolInstructionData = { discriminator: ReadonlyUint8Array };

export type LockPoolInstructionDataArgs = {};

export function getLockPoolInstructionDataEncoder(): FixedSizeEncoder<LockPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LOCK_POOL_DISCRIMINATOR }),
  );
}

export function getLockPoolInstructionDataDecoder(): FixedSizeDecoder<LockPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getLockPoolInstructionDataCodec(): FixedSizeCodec<
  LockPoolInstructionDataArgs,
  LockPoolInstructionData
> {
  return combineCodec(
    getLockPoolInstructionDataEncoder(),
    getLockPoolInstructionDataDecoder(),
  );
}

export type LockPoolInput<
  TAccountAuthority extends string = string,
  TAccountPool extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  pool: Address<TAccountPool>;
};

export function getLockPoolInstruction<
  TAccountAuthority extends string,
  TAccountPool extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
>(
  input: LockPoolInput<TAccountAuthority, TAccountPool>,
  config?: { programAddress?: TProgramAddress },
): LockPoolInstruction<TProgramAddress, TAccountAuthority, TAccountPool> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_BETTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.pool),
    ],
    data: getLockPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as LockPoolInstruction<TProgramAddress, TAccountAuthority, TAccountPool>);
}

export type ParsedLockPoolInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    pool: TAccountMetas[1];
  };
  data: LockPoolInstructionData;
};

export function parseLockPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLockPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), pool: getNextAccount() },
    data: getLockPoolInstructionDataDecoder().decode(instruction.data),
  };
}
