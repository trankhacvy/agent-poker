/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_BETTING_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SETTLE_POOL_DISCRIMINATOR = new Uint8Array([
  186, 11, 231, 111, 242, 241, 203, 64,
]);

export function getSettlePoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SETTLE_POOL_DISCRIMINATOR);
}

export type SettlePoolInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountPoolVault extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountPoolVault extends string
        ? WritableAccount<TAccountPoolVault>
        : TAccountPoolVault,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SettlePoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  winnerIndex: number;
};

export type SettlePoolInstructionDataArgs = { winnerIndex: number };

export function getSettlePoolInstructionDataEncoder(): FixedSizeEncoder<SettlePoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["winnerIndex", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SETTLE_POOL_DISCRIMINATOR }),
  );
}

export function getSettlePoolInstructionDataDecoder(): FixedSizeDecoder<SettlePoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["winnerIndex", getU8Decoder()],
  ]);
}

export function getSettlePoolInstructionDataCodec(): FixedSizeCodec<
  SettlePoolInstructionDataArgs,
  SettlePoolInstructionData
> {
  return combineCodec(
    getSettlePoolInstructionDataEncoder(),
    getSettlePoolInstructionDataDecoder(),
  );
}

export type SettlePoolAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPool extends string = string,
  TAccountPoolVault extends string = string,
  TAccountTreasury extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  pool: Address<TAccountPool>;
  poolVault?: Address<TAccountPoolVault>;
  treasury?: Address<TAccountTreasury>;
  systemProgram?: Address<TAccountSystemProgram>;
  winnerIndex: SettlePoolInstructionDataArgs["winnerIndex"];
};

export async function getSettlePoolInstructionAsync<
  TAccountAuthority extends string,
  TAccountPool extends string,
  TAccountPoolVault extends string,
  TAccountTreasury extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
>(
  input: SettlePoolAsyncInput<
    TAccountAuthority,
    TAccountPool,
    TAccountPoolVault,
    TAccountTreasury,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettlePoolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPool,
    TAccountPoolVault,
    TAccountTreasury,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_BETTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    poolVault: { value: input.poolVault ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.poolVault.value) {
    accounts.poolVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 111, 108, 95, 118, 97, 117, 108, 116]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.pool.value)),
      ],
    });
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 101, 97, 115, 117, 114, 121]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.poolVault),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSettlePoolInstructionDataEncoder().encode(
      args as SettlePoolInstructionDataArgs,
    ),
    programAddress,
  } as SettlePoolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPool,
    TAccountPoolVault,
    TAccountTreasury,
    TAccountSystemProgram
  >);
}

export type SettlePoolInput<
  TAccountAuthority extends string = string,
  TAccountPool extends string = string,
  TAccountPoolVault extends string = string,
  TAccountTreasury extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  pool: Address<TAccountPool>;
  poolVault: Address<TAccountPoolVault>;
  treasury: Address<TAccountTreasury>;
  systemProgram?: Address<TAccountSystemProgram>;
  winnerIndex: SettlePoolInstructionDataArgs["winnerIndex"];
};

export function getSettlePoolInstruction<
  TAccountAuthority extends string,
  TAccountPool extends string,
  TAccountPoolVault extends string,
  TAccountTreasury extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
>(
  input: SettlePoolInput<
    TAccountAuthority,
    TAccountPool,
    TAccountPoolVault,
    TAccountTreasury,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SettlePoolInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountPool,
  TAccountPoolVault,
  TAccountTreasury,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_BETTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    poolVault: { value: input.poolVault ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.poolVault),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSettlePoolInstructionDataEncoder().encode(
      args as SettlePoolInstructionDataArgs,
    ),
    programAddress,
  } as SettlePoolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPool,
    TAccountPoolVault,
    TAccountTreasury,
    TAccountSystemProgram
  >);
}

export type ParsedSettlePoolInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    pool: TAccountMetas[1];
    poolVault: TAccountMetas[2];
    treasury: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: SettlePoolInstructionData;
};

export function parseSettlePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettlePoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      pool: getNextAccount(),
      poolVault: getNextAccount(),
      treasury: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSettlePoolInstructionDataDecoder().decode(instruction.data),
  };
}
