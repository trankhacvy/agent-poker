/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_BETTING_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CANCEL_POOL_DISCRIMINATOR = new Uint8Array([
  211, 11, 27, 100, 252, 115, 57, 77,
]);

export function getCancelPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CANCEL_POOL_DISCRIMINATOR);
}

export type CancelPoolInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      ...TRemainingAccounts,
    ]
  >;

export type CancelPoolInstructionData = { discriminator: ReadonlyUint8Array };

export type CancelPoolInstructionDataArgs = {};

export function getCancelPoolInstructionDataEncoder(): FixedSizeEncoder<CancelPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_POOL_DISCRIMINATOR }),
  );
}

export function getCancelPoolInstructionDataDecoder(): FixedSizeDecoder<CancelPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelPoolInstructionDataCodec(): FixedSizeCodec<
  CancelPoolInstructionDataArgs,
  CancelPoolInstructionData
> {
  return combineCodec(
    getCancelPoolInstructionDataEncoder(),
    getCancelPoolInstructionDataDecoder(),
  );
}

export type CancelPoolInput<
  TAccountAuthority extends string = string,
  TAccountPool extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  pool: Address<TAccountPool>;
};

export function getCancelPoolInstruction<
  TAccountAuthority extends string,
  TAccountPool extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
>(
  input: CancelPoolInput<TAccountAuthority, TAccountPool>,
  config?: { programAddress?: TProgramAddress },
): CancelPoolInstruction<TProgramAddress, TAccountAuthority, TAccountPool> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_BETTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.pool),
    ],
    data: getCancelPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelPoolInstruction<TProgramAddress, TAccountAuthority, TAccountPool>);
}

export type ParsedCancelPoolInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    pool: TAccountMetas[1];
  };
  data: CancelPoolInstructionData;
};

export function parseCancelPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), pool: getNextAccount() },
    data: getCancelPoolInstructionDataDecoder().decode(instruction.data),
  };
}
