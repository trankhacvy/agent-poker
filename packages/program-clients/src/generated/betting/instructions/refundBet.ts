/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_BETTING_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REFUND_BET_DISCRIMINATOR = new Uint8Array([
  209, 182, 226, 96, 55, 121, 83, 183,
]);

export function getRefundBetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REFUND_BET_DISCRIMINATOR);
}

export type RefundBetInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountBettor extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountPoolVault extends string | AccountMeta<string> = string,
  TAccountBet extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBettor extends string
        ? WritableAccount<TAccountBettor>
        : TAccountBettor,
      TAccountPool extends string
        ? ReadonlyAccount<TAccountPool>
        : TAccountPool,
      TAccountPoolVault extends string
        ? WritableAccount<TAccountPoolVault>
        : TAccountPoolVault,
      TAccountBet extends string ? WritableAccount<TAccountBet> : TAccountBet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundBetInstructionData = { discriminator: ReadonlyUint8Array };

export type RefundBetInstructionDataArgs = {};

export function getRefundBetInstructionDataEncoder(): FixedSizeEncoder<RefundBetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REFUND_BET_DISCRIMINATOR }),
  );
}

export function getRefundBetInstructionDataDecoder(): FixedSizeDecoder<RefundBetInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundBetInstructionDataCodec(): FixedSizeCodec<
  RefundBetInstructionDataArgs,
  RefundBetInstructionData
> {
  return combineCodec(
    getRefundBetInstructionDataEncoder(),
    getRefundBetInstructionDataDecoder(),
  );
}

export type RefundBetAsyncInput<
  TAccountBettor extends string = string,
  TAccountPool extends string = string,
  TAccountPoolVault extends string = string,
  TAccountBet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  bettor: Address<TAccountBettor>;
  pool: Address<TAccountPool>;
  poolVault?: Address<TAccountPoolVault>;
  bet?: Address<TAccountBet>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRefundBetInstructionAsync<
  TAccountBettor extends string,
  TAccountPool extends string,
  TAccountPoolVault extends string,
  TAccountBet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
>(
  input: RefundBetAsyncInput<
    TAccountBettor,
    TAccountPool,
    TAccountPoolVault,
    TAccountBet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RefundBetInstruction<
    TProgramAddress,
    TAccountBettor,
    TAccountPool,
    TAccountPoolVault,
    TAccountBet,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_BETTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bettor: { value: input.bettor ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: false },
    poolVault: { value: input.poolVault ?? null, isWritable: true },
    bet: { value: input.bet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.poolVault.value) {
    accounts.poolVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 111, 108, 95, 118, 97, 117, 108, 116]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.pool.value)),
      ],
    });
  }
  if (!accounts.bet.value) {
    accounts.bet.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([98, 101, 116])),
        getAddressEncoder().encode(expectAddress(accounts.pool.value)),
        getAddressEncoder().encode(expectAddress(accounts.bettor.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.bettor),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.poolVault),
      getAccountMeta(accounts.bet),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRefundBetInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundBetInstruction<
    TProgramAddress,
    TAccountBettor,
    TAccountPool,
    TAccountPoolVault,
    TAccountBet,
    TAccountSystemProgram
  >);
}

export type RefundBetInput<
  TAccountBettor extends string = string,
  TAccountPool extends string = string,
  TAccountPoolVault extends string = string,
  TAccountBet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  bettor: Address<TAccountBettor>;
  pool: Address<TAccountPool>;
  poolVault: Address<TAccountPoolVault>;
  bet: Address<TAccountBet>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRefundBetInstruction<
  TAccountBettor extends string,
  TAccountPool extends string,
  TAccountPoolVault extends string,
  TAccountBet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
>(
  input: RefundBetInput<
    TAccountBettor,
    TAccountPool,
    TAccountPoolVault,
    TAccountBet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RefundBetInstruction<
  TProgramAddress,
  TAccountBettor,
  TAccountPool,
  TAccountPoolVault,
  TAccountBet,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_BETTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bettor: { value: input.bettor ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: false },
    poolVault: { value: input.poolVault ?? null, isWritable: true },
    bet: { value: input.bet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.bettor),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.poolVault),
      getAccountMeta(accounts.bet),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRefundBetInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundBetInstruction<
    TProgramAddress,
    TAccountBettor,
    TAccountPool,
    TAccountPoolVault,
    TAccountBet,
    TAccountSystemProgram
  >);
}

export type ParsedRefundBetInstruction<
  TProgram extends string = typeof AGENT_POKER_BETTING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    bettor: TAccountMetas[0];
    pool: TAccountMetas[1];
    poolVault: TAccountMetas[2];
    bet: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RefundBetInstructionData;
};

export function parseRefundBetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundBetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      bettor: getNextAccount(),
      pool: getNextAccount(),
      poolVault: getNextAccount(),
      bet: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundBetInstructionDataDecoder().decode(instruction.data),
  };
}
