/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_AGENT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const UPDATE_STATS_DISCRIMINATOR = new Uint8Array([
  145, 138, 9, 150, 178, 31, 158, 244,
]);

export function getUpdateStatsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_STATS_DISCRIMINATOR,
  );
}

export type UpdateStatsInstruction<
  TProgram extends string = typeof AGENT_POKER_AGENT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAgent extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAgent extends string
        ? WritableAccount<TAccountAgent>
        : TAccountAgent,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateStatsInstructionData = {
  discriminator: ReadonlyUint8Array;
  gamesDelta: bigint;
  winsDelta: bigint;
  earningsDelta: bigint;
};

export type UpdateStatsInstructionDataArgs = {
  gamesDelta: number | bigint;
  winsDelta: number | bigint;
  earningsDelta: number | bigint;
};

export function getUpdateStatsInstructionDataEncoder(): FixedSizeEncoder<UpdateStatsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gamesDelta", getU64Encoder()],
      ["winsDelta", getU64Encoder()],
      ["earningsDelta", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_STATS_DISCRIMINATOR }),
  );
}

export function getUpdateStatsInstructionDataDecoder(): FixedSizeDecoder<UpdateStatsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gamesDelta", getU64Decoder()],
    ["winsDelta", getU64Decoder()],
    ["earningsDelta", getI64Decoder()],
  ]);
}

export function getUpdateStatsInstructionDataCodec(): FixedSizeCodec<
  UpdateStatsInstructionDataArgs,
  UpdateStatsInstructionData
> {
  return combineCodec(
    getUpdateStatsInstructionDataEncoder(),
    getUpdateStatsInstructionDataDecoder(),
  );
}

export type UpdateStatsInput<
  TAccountAuthority extends string = string,
  TAccountAgent extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  agent: Address<TAccountAgent>;
  gamesDelta: UpdateStatsInstructionDataArgs["gamesDelta"];
  winsDelta: UpdateStatsInstructionDataArgs["winsDelta"];
  earningsDelta: UpdateStatsInstructionDataArgs["earningsDelta"];
};

export function getUpdateStatsInstruction<
  TAccountAuthority extends string,
  TAccountAgent extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_AGENT_PROGRAM_ADDRESS,
>(
  input: UpdateStatsInput<TAccountAuthority, TAccountAgent>,
  config?: { programAddress?: TProgramAddress },
): UpdateStatsInstruction<TProgramAddress, TAccountAuthority, TAccountAgent> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_AGENT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    agent: { value: input.agent ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.agent),
    ],
    data: getUpdateStatsInstructionDataEncoder().encode(
      args as UpdateStatsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateStatsInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAgent
  >);
}

export type ParsedUpdateStatsInstruction<
  TProgram extends string = typeof AGENT_POKER_AGENT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    agent: TAccountMetas[1];
  };
  data: UpdateStatsInstructionData;
};

export function parseUpdateStatsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateStatsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), agent: getNextAccount() },
    data: getUpdateStatsInstructionDataDecoder().decode(instruction.data),
  };
}
