/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { AGENT_POKER_AGENT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_AGENT_DISCRIMINATOR = new Uint8Array([
  143, 66, 198, 95, 110, 85, 83, 249,
]);

export function getCreateAgentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_AGENT_DISCRIMINATOR,
  );
}

export type CreateAgentInstruction<
  TProgram extends string = typeof AGENT_POKER_AGENT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountAgent extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountAgent extends string
        ? WritableAccount<TAccountAgent>
        : TAccountAgent,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateAgentInstructionData = {
  discriminator: ReadonlyUint8Array;
  template: number;
  displayName: string;
};

export type CreateAgentInstructionDataArgs = {
  template: number;
  displayName: string;
};

export function getCreateAgentInstructionDataEncoder(): Encoder<CreateAgentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["template", getU8Encoder()],
      ["displayName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_AGENT_DISCRIMINATOR }),
  );
}

export function getCreateAgentInstructionDataDecoder(): Decoder<CreateAgentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["template", getU8Decoder()],
    ["displayName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateAgentInstructionDataCodec(): Codec<
  CreateAgentInstructionDataArgs,
  CreateAgentInstructionData
> {
  return combineCodec(
    getCreateAgentInstructionDataEncoder(),
    getCreateAgentInstructionDataDecoder(),
  );
}

export type CreateAgentAsyncInput<
  TAccountOwner extends string = string,
  TAccountAgent extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  agent?: Address<TAccountAgent>;
  vault?: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  template: CreateAgentInstructionDataArgs["template"];
  displayName: CreateAgentInstructionDataArgs["displayName"];
};

export async function getCreateAgentInstructionAsync<
  TAccountOwner extends string,
  TAccountAgent extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_AGENT_PROGRAM_ADDRESS,
>(
  input: CreateAgentAsyncInput<
    TAccountOwner,
    TAccountAgent,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateAgentInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountAgent,
    TAccountVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_AGENT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.agent.value) {
    accounts.agent.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([97, 103, 101, 110, 116])),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([97, 103, 101, 110, 116, 95, 118, 97, 117, 108, 116]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.agent),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateAgentInstructionDataEncoder().encode(
      args as CreateAgentInstructionDataArgs,
    ),
    programAddress,
  } as CreateAgentInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountAgent,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type CreateAgentInput<
  TAccountOwner extends string = string,
  TAccountAgent extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  agent: Address<TAccountAgent>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  template: CreateAgentInstructionDataArgs["template"];
  displayName: CreateAgentInstructionDataArgs["displayName"];
};

export function getCreateAgentInstruction<
  TAccountOwner extends string,
  TAccountAgent extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_AGENT_PROGRAM_ADDRESS,
>(
  input: CreateAgentInput<
    TAccountOwner,
    TAccountAgent,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateAgentInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountAgent,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_AGENT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.agent),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateAgentInstructionDataEncoder().encode(
      args as CreateAgentInstructionDataArgs,
    ),
    programAddress,
  } as CreateAgentInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountAgent,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedCreateAgentInstruction<
  TProgram extends string = typeof AGENT_POKER_AGENT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    agent: TAccountMetas[1];
    vault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateAgentInstructionData;
};

export function parseCreateAgentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateAgentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      agent: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateAgentInstructionDataDecoder().decode(instruction.data),
  };
}
