/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_ESCROW_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REFUND_SESSION_DISCRIMINATOR = new Uint8Array([
  168, 87, 185, 45, 250, 170, 10, 85,
]);

export function getRefundSessionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_SESSION_DISCRIMINATOR,
  );
}

export type RefundSessionInstruction<
  TProgram extends string = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountSessionVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      TAccountSessionVault extends string
        ? WritableAccount<TAccountSessionVault>
        : TAccountSessionVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RefundSessionInstructionDataArgs = {};

export function getRefundSessionInstructionDataEncoder(): FixedSizeEncoder<RefundSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REFUND_SESSION_DISCRIMINATOR }),
  );
}

export function getRefundSessionInstructionDataDecoder(): FixedSizeDecoder<RefundSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundSessionInstructionDataCodec(): FixedSizeCodec<
  RefundSessionInstructionDataArgs,
  RefundSessionInstructionData
> {
  return combineCodec(
    getRefundSessionInstructionDataEncoder(),
    getRefundSessionInstructionDataDecoder(),
  );
}

export type RefundSessionInput<
  TAccountAuthority extends string = string,
  TAccountSession extends string = string,
  TAccountSessionVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  session: Address<TAccountSession>;
  sessionVault: Address<TAccountSessionVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRefundSessionInstruction<
  TAccountAuthority extends string,
  TAccountSession extends string,
  TAccountSessionVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
>(
  input: RefundSessionInput<
    TAccountAuthority,
    TAccountSession,
    TAccountSessionVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RefundSessionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSession,
  TAccountSessionVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
    sessionVault: { value: input.sessionVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.session),
      getAccountMeta(accounts.sessionVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRefundSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundSessionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSession,
    TAccountSessionVault,
    TAccountSystemProgram
  >);
}

export type ParsedRefundSessionInstruction<
  TProgram extends string = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    session: TAccountMetas[1];
    sessionVault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RefundSessionInstructionData;
};

export function parseRefundSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      session: getNextAccount(),
      sessionVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundSessionInstructionDataDecoder().decode(instruction.data),
  };
}
