/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_ESCROW_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SETTLE_TABLE_DISCRIMINATOR = new Uint8Array([
  137, 147, 185, 185, 80, 61, 145, 115,
]);

export function getSettleTableDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_TABLE_DISCRIMINATOR,
  );
}

export type SettleTableInstruction<
  TProgram extends string = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTable extends string | AccountMeta<string> = string,
  TAccountTableVault extends string | AccountMeta<string> = string,
  TAccountWinnerVault extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTable extends string
        ? WritableAccount<TAccountTable>
        : TAccountTable,
      TAccountTableVault extends string
        ? WritableAccount<TAccountTableVault>
        : TAccountTableVault,
      TAccountWinnerVault extends string
        ? WritableAccount<TAccountWinnerVault>
        : TAccountWinnerVault,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SettleTableInstructionData = {
  discriminator: ReadonlyUint8Array;
  winnerIndex: number;
};

export type SettleTableInstructionDataArgs = { winnerIndex: number };

export function getSettleTableInstructionDataEncoder(): FixedSizeEncoder<SettleTableInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["winnerIndex", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SETTLE_TABLE_DISCRIMINATOR }),
  );
}

export function getSettleTableInstructionDataDecoder(): FixedSizeDecoder<SettleTableInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["winnerIndex", getU8Decoder()],
  ]);
}

export function getSettleTableInstructionDataCodec(): FixedSizeCodec<
  SettleTableInstructionDataArgs,
  SettleTableInstructionData
> {
  return combineCodec(
    getSettleTableInstructionDataEncoder(),
    getSettleTableInstructionDataDecoder(),
  );
}

export type SettleTableAsyncInput<
  TAccountAuthority extends string = string,
  TAccountTable extends string = string,
  TAccountTableVault extends string = string,
  TAccountWinnerVault extends string = string,
  TAccountTreasury extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  table: Address<TAccountTable>;
  /** Lamports are manipulated directly since this program controls the PDA. */
  tableVault: Address<TAccountTableVault>;
  /** Address is validated against the winner's pubkey stored in the table. */
  winnerVault: Address<TAccountWinnerVault>;
  treasury?: Address<TAccountTreasury>;
  systemProgram?: Address<TAccountSystemProgram>;
  winnerIndex: SettleTableInstructionDataArgs["winnerIndex"];
};

export async function getSettleTableInstructionAsync<
  TAccountAuthority extends string,
  TAccountTable extends string,
  TAccountTableVault extends string,
  TAccountWinnerVault extends string,
  TAccountTreasury extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
>(
  input: SettleTableAsyncInput<
    TAccountAuthority,
    TAccountTable,
    TAccountTableVault,
    TAccountWinnerVault,
    TAccountTreasury,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleTableInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTable,
    TAccountTableVault,
    TAccountWinnerVault,
    TAccountTreasury,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    table: { value: input.table ?? null, isWritable: true },
    tableVault: { value: input.tableVault ?? null, isWritable: true },
    winnerVault: { value: input.winnerVault ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.treasury.value) {
    accounts.treasury.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 101, 97, 115, 117, 114, 121]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.table),
      getAccountMeta(accounts.tableVault),
      getAccountMeta(accounts.winnerVault),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSettleTableInstructionDataEncoder().encode(
      args as SettleTableInstructionDataArgs,
    ),
    programAddress,
  } as SettleTableInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTable,
    TAccountTableVault,
    TAccountWinnerVault,
    TAccountTreasury,
    TAccountSystemProgram
  >);
}

export type SettleTableInput<
  TAccountAuthority extends string = string,
  TAccountTable extends string = string,
  TAccountTableVault extends string = string,
  TAccountWinnerVault extends string = string,
  TAccountTreasury extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  table: Address<TAccountTable>;
  /** Lamports are manipulated directly since this program controls the PDA. */
  tableVault: Address<TAccountTableVault>;
  /** Address is validated against the winner's pubkey stored in the table. */
  winnerVault: Address<TAccountWinnerVault>;
  treasury: Address<TAccountTreasury>;
  systemProgram?: Address<TAccountSystemProgram>;
  winnerIndex: SettleTableInstructionDataArgs["winnerIndex"];
};

export function getSettleTableInstruction<
  TAccountAuthority extends string,
  TAccountTable extends string,
  TAccountTableVault extends string,
  TAccountWinnerVault extends string,
  TAccountTreasury extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
>(
  input: SettleTableInput<
    TAccountAuthority,
    TAccountTable,
    TAccountTableVault,
    TAccountWinnerVault,
    TAccountTreasury,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SettleTableInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountTable,
  TAccountTableVault,
  TAccountWinnerVault,
  TAccountTreasury,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    table: { value: input.table ?? null, isWritable: true },
    tableVault: { value: input.tableVault ?? null, isWritable: true },
    winnerVault: { value: input.winnerVault ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.table),
      getAccountMeta(accounts.tableVault),
      getAccountMeta(accounts.winnerVault),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSettleTableInstructionDataEncoder().encode(
      args as SettleTableInstructionDataArgs,
    ),
    programAddress,
  } as SettleTableInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTable,
    TAccountTableVault,
    TAccountWinnerVault,
    TAccountTreasury,
    TAccountSystemProgram
  >);
}

export type ParsedSettleTableInstruction<
  TProgram extends string = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    table: TAccountMetas[1];
    /** Lamports are manipulated directly since this program controls the PDA. */
    tableVault: TAccountMetas[2];
    /** Address is validated against the winner's pubkey stored in the table. */
    winnerVault: TAccountMetas[3];
    treasury: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: SettleTableInstructionData;
};

export function parseSettleTableInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleTableInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      table: getNextAccount(),
      tableVault: getNextAccount(),
      winnerVault: getNextAccount(),
      treasury: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSettleTableInstructionDataDecoder().decode(instruction.data),
  };
}
