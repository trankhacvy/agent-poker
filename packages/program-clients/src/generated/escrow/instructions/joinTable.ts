/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { AGENT_POKER_ESCROW_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const JOIN_TABLE_DISCRIMINATOR = new Uint8Array([
  14, 117, 84, 51, 95, 146, 171, 70,
]);

export function getJoinTableDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(JOIN_TABLE_DISCRIMINATOR);
}

export type JoinTableInstruction<
  TProgram extends string = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
  TAccountAgentOwner extends string | AccountMeta<string> = string,
  TAccountTable extends string | AccountMeta<string> = string,
  TAccountTableVault extends string | AccountMeta<string> = string,
  TAccountAgentVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAgentOwner extends string
        ? WritableSignerAccount<TAccountAgentOwner> &
            AccountSignerMeta<TAccountAgentOwner>
        : TAccountAgentOwner,
      TAccountTable extends string
        ? WritableAccount<TAccountTable>
        : TAccountTable,
      TAccountTableVault extends string
        ? WritableAccount<TAccountTableVault>
        : TAccountTableVault,
      TAccountAgentVault extends string
        ? WritableAccount<TAccountAgentVault>
        : TAccountAgentVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type JoinTableInstructionData = {
  discriminator: ReadonlyUint8Array;
  agentVaultBump: number;
};

export type JoinTableInstructionDataArgs = { agentVaultBump: number };

export function getJoinTableInstructionDataEncoder(): FixedSizeEncoder<JoinTableInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["agentVaultBump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: JOIN_TABLE_DISCRIMINATOR }),
  );
}

export function getJoinTableInstructionDataDecoder(): FixedSizeDecoder<JoinTableInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["agentVaultBump", getU8Decoder()],
  ]);
}

export function getJoinTableInstructionDataCodec(): FixedSizeCodec<
  JoinTableInstructionDataArgs,
  JoinTableInstructionData
> {
  return combineCodec(
    getJoinTableInstructionDataEncoder(),
    getJoinTableInstructionDataDecoder(),
  );
}

export type JoinTableAsyncInput<
  TAccountAgentOwner extends string = string,
  TAccountTable extends string = string,
  TAccountTableVault extends string = string,
  TAccountAgentVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The owner of the agent. Must sign to authorize the wager transfer. */
  agentOwner: TransactionSigner<TAccountAgentOwner>;
  table: Address<TAccountTable>;
  tableVault: Address<TAccountTableVault>;
  /** Address is validated by the seeds constraint with seeds::program. */
  agentVault?: Address<TAccountAgentVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  agentVaultBump: JoinTableInstructionDataArgs["agentVaultBump"];
};

export async function getJoinTableInstructionAsync<
  TAccountAgentOwner extends string,
  TAccountTable extends string,
  TAccountTableVault extends string,
  TAccountAgentVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
>(
  input: JoinTableAsyncInput<
    TAccountAgentOwner,
    TAccountTable,
    TAccountTableVault,
    TAccountAgentVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  JoinTableInstruction<
    TProgramAddress,
    TAccountAgentOwner,
    TAccountTable,
    TAccountTableVault,
    TAccountAgentVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agentOwner: { value: input.agentOwner ?? null, isWritable: true },
    table: { value: input.table ?? null, isWritable: true },
    tableVault: { value: input.tableVault ?? null, isWritable: true },
    agentVault: { value: input.agentVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.agentVault.value) {
    accounts.agentVault.value = await getProgramDerivedAddress({
      programAddress:
        "6xJviS1Mz3rArD3JciQ55u7K1xDqtYr1AGvSeWvW1dti" as Address<"6xJviS1Mz3rArD3JciQ55u7K1xDqtYr1AGvSeWvW1dti">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([97, 103, 101, 110, 116, 95, 118, 97, 117, 108, 116]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.agentOwner.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.agentOwner),
      getAccountMeta(accounts.table),
      getAccountMeta(accounts.tableVault),
      getAccountMeta(accounts.agentVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getJoinTableInstructionDataEncoder().encode(
      args as JoinTableInstructionDataArgs,
    ),
    programAddress,
  } as JoinTableInstruction<
    TProgramAddress,
    TAccountAgentOwner,
    TAccountTable,
    TAccountTableVault,
    TAccountAgentVault,
    TAccountSystemProgram
  >);
}

export type JoinTableInput<
  TAccountAgentOwner extends string = string,
  TAccountTable extends string = string,
  TAccountTableVault extends string = string,
  TAccountAgentVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The owner of the agent. Must sign to authorize the wager transfer. */
  agentOwner: TransactionSigner<TAccountAgentOwner>;
  table: Address<TAccountTable>;
  tableVault: Address<TAccountTableVault>;
  /** Address is validated by the seeds constraint with seeds::program. */
  agentVault: Address<TAccountAgentVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  agentVaultBump: JoinTableInstructionDataArgs["agentVaultBump"];
};

export function getJoinTableInstruction<
  TAccountAgentOwner extends string,
  TAccountTable extends string,
  TAccountTableVault extends string,
  TAccountAgentVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
>(
  input: JoinTableInput<
    TAccountAgentOwner,
    TAccountTable,
    TAccountTableVault,
    TAccountAgentVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): JoinTableInstruction<
  TProgramAddress,
  TAccountAgentOwner,
  TAccountTable,
  TAccountTableVault,
  TAccountAgentVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agentOwner: { value: input.agentOwner ?? null, isWritable: true },
    table: { value: input.table ?? null, isWritable: true },
    tableVault: { value: input.tableVault ?? null, isWritable: true },
    agentVault: { value: input.agentVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.agentOwner),
      getAccountMeta(accounts.table),
      getAccountMeta(accounts.tableVault),
      getAccountMeta(accounts.agentVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getJoinTableInstructionDataEncoder().encode(
      args as JoinTableInstructionDataArgs,
    ),
    programAddress,
  } as JoinTableInstruction<
    TProgramAddress,
    TAccountAgentOwner,
    TAccountTable,
    TAccountTableVault,
    TAccountAgentVault,
    TAccountSystemProgram
  >);
}

export type ParsedJoinTableInstruction<
  TProgram extends string = typeof AGENT_POKER_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The owner of the agent. Must sign to authorize the wager transfer. */
    agentOwner: TAccountMetas[0];
    table: TAccountMetas[1];
    tableVault: TAccountMetas[2];
    /** Address is validated by the seeds constraint with seeds::program. */
    agentVault: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: JoinTableInstructionData;
};

export function parseJoinTableInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedJoinTableInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      agentOwner: getNextAccount(),
      table: getNextAccount(),
      tableVault: getNextAccount(),
      agentVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getJoinTableInstructionDataDecoder().decode(instruction.data),
  };
}
