/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getTableStatusDecoder,
  getTableStatusEncoder,
  type TableStatus,
  type TableStatusArgs,
} from "../types";

export const TABLE_ESCROW_DISCRIMINATOR = new Uint8Array([
  200, 120, 2, 103, 194, 160, 7, 29,
]);

export function getTableEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TABLE_ESCROW_DISCRIMINATOR,
  );
}

export type TableEscrow = {
  discriminator: ReadonlyUint8Array;
  /** Unique identifier for this table. */
  tableId: bigint;
  /** Wager amount in lamports each player must deposit. */
  wagerTier: bigint;
  /** Array of player pubkeys (agent owners). Unused slots are Pubkey::default(). */
  players: Array<Address>;
  /** Number of players currently seated. */
  playerCount: number;
  /** Current status of the table. */
  status: TableStatus;
  /** The winner's pubkey, set after settlement. */
  winner: Option<Address>;
  /** Authority who created and manages this table. */
  authority: Address;
  /** Unix timestamp when the table was created. */
  createdAt: bigint;
  /** PDA bump for the table account. */
  bump: number;
  /** PDA bump for the table vault account. */
  vaultBump: number;
};

export type TableEscrowArgs = {
  /** Unique identifier for this table. */
  tableId: number | bigint;
  /** Wager amount in lamports each player must deposit. */
  wagerTier: number | bigint;
  /** Array of player pubkeys (agent owners). Unused slots are Pubkey::default(). */
  players: Array<Address>;
  /** Number of players currently seated. */
  playerCount: number;
  /** Current status of the table. */
  status: TableStatusArgs;
  /** The winner's pubkey, set after settlement. */
  winner: OptionOrNullable<Address>;
  /** Authority who created and manages this table. */
  authority: Address;
  /** Unix timestamp when the table was created. */
  createdAt: number | bigint;
  /** PDA bump for the table account. */
  bump: number;
  /** PDA bump for the table vault account. */
  vaultBump: number;
};

/** Gets the encoder for {@link TableEscrowArgs} account data. */
export function getTableEscrowEncoder(): Encoder<TableEscrowArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tableId", getU64Encoder()],
      ["wagerTier", getU64Encoder()],
      ["players", getArrayEncoder(getAddressEncoder(), { size: 6 })],
      ["playerCount", getU8Encoder()],
      ["status", getTableStatusEncoder()],
      ["winner", getOptionEncoder(getAddressEncoder())],
      ["authority", getAddressEncoder()],
      ["createdAt", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["vaultBump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TABLE_ESCROW_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TableEscrow} account data. */
export function getTableEscrowDecoder(): Decoder<TableEscrow> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tableId", getU64Decoder()],
    ["wagerTier", getU64Decoder()],
    ["players", getArrayDecoder(getAddressDecoder(), { size: 6 })],
    ["playerCount", getU8Decoder()],
    ["status", getTableStatusDecoder()],
    ["winner", getOptionDecoder(getAddressDecoder())],
    ["authority", getAddressDecoder()],
    ["createdAt", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["vaultBump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link TableEscrow} account data. */
export function getTableEscrowCodec(): Codec<TableEscrowArgs, TableEscrow> {
  return combineCodec(getTableEscrowEncoder(), getTableEscrowDecoder());
}

export function decodeTableEscrow<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TableEscrow, TAddress>;
export function decodeTableEscrow<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TableEscrow, TAddress>;
export function decodeTableEscrow<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<TableEscrow, TAddress> | MaybeAccount<TableEscrow, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTableEscrowDecoder(),
  );
}

export async function fetchTableEscrow<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TableEscrow, TAddress>> {
  const maybeAccount = await fetchMaybeTableEscrow(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTableEscrow<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TableEscrow, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTableEscrow(maybeAccount);
}

export async function fetchAllTableEscrow(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TableEscrow>[]> {
  const maybeAccounts = await fetchAllMaybeTableEscrow(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTableEscrow(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TableEscrow>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTableEscrow(maybeAccount));
}
