/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { AGENT_POKER_GAME_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SHOWDOWN_DISCRIMINATOR = new Uint8Array([
  42, 62, 227, 166, 247, 144, 182, 162,
]);

export function getShowdownDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SHOWDOWN_DISCRIMINATOR);
}

export type ShowdownInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> =
    "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1",
  TAccountMagicProgram extends string | AccountMeta<string> =
    "Magic11111111111111111111111111111111111111",
  TAccountMagicContext extends string | AccountMeta<string> =
    "MagicContext1111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      ...TRemainingAccounts,
    ]
  >;

export type ShowdownInstructionData = { discriminator: ReadonlyUint8Array };

export type ShowdownInstructionDataArgs = {};

export function getShowdownInstructionDataEncoder(): FixedSizeEncoder<ShowdownInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SHOWDOWN_DISCRIMINATOR }),
  );
}

export function getShowdownInstructionDataDecoder(): FixedSizeDecoder<ShowdownInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getShowdownInstructionDataCodec(): FixedSizeCodec<
  ShowdownInstructionDataArgs,
  ShowdownInstructionData
> {
  return combineCodec(
    getShowdownInstructionDataEncoder(),
    getShowdownInstructionDataDecoder(),
  );
}

export type ShowdownInput<
  TAccountPayer extends string = string,
  TAccountGame extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountMagicContext extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  game: Address<TAccountGame>;
  permissionProgram?: Address<TAccountPermissionProgram>;
  magicProgram?: Address<TAccountMagicProgram>;
  magicContext?: Address<TAccountMagicContext>;
};

export function getShowdownInstruction<
  TAccountPayer extends string,
  TAccountGame extends string,
  TAccountPermissionProgram extends string,
  TAccountMagicProgram extends string,
  TAccountMagicContext extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: ShowdownInput<
    TAccountPayer,
    TAccountGame,
    TAccountPermissionProgram,
    TAccountMagicProgram,
    TAccountMagicContext
  >,
  config?: { programAddress?: TProgramAddress },
): ShowdownInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountGame,
  TAccountPermissionProgram,
  TAccountMagicProgram,
  TAccountMagicContext
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.permissionProgram.value) {
    accounts.permissionProgram.value =
      "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1" as Address<"ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1">;
  }
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.magicContext.value) {
    accounts.magicContext.value =
      "MagicContext1111111111111111111111111111111" as Address<"MagicContext1111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.magicProgram),
      getAccountMeta(accounts.magicContext),
    ],
    data: getShowdownInstructionDataEncoder().encode({}),
    programAddress,
  } as ShowdownInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountGame,
    TAccountPermissionProgram,
    TAccountMagicProgram,
    TAccountMagicContext
  >);
}

export type ParsedShowdownInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    game: TAccountMetas[1];
    permissionProgram: TAccountMetas[2];
    magicProgram: TAccountMetas[3];
    magicContext: TAccountMetas[4];
  };
  data: ShowdownInstructionData;
};

export function parseShowdownInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedShowdownInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      game: getNextAccount(),
      permissionProgram: getNextAccount(),
      magicProgram: getNextAccount(),
      magicContext: getNextAccount(),
    },
    data: getShowdownInstructionDataDecoder().decode(instruction.data),
  };
}
