/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_GAME_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const PLAYER_ACTION_DISCRIMINATOR = new Uint8Array([
  37, 85, 25, 135, 200, 116, 96, 101,
]);

export function getPlayerActionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PLAYER_ACTION_DISCRIMINATOR,
  );
}

export type PlayerActionInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      ...TRemainingAccounts,
    ]
  >;

export type PlayerActionInstructionData = {
  discriminator: ReadonlyUint8Array;
  action: number;
  raiseAmount: bigint;
};

export type PlayerActionInstructionDataArgs = {
  action: number;
  raiseAmount: number | bigint;
};

export function getPlayerActionInstructionDataEncoder(): FixedSizeEncoder<PlayerActionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["action", getU8Encoder()],
      ["raiseAmount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PLAYER_ACTION_DISCRIMINATOR }),
  );
}

export function getPlayerActionInstructionDataDecoder(): FixedSizeDecoder<PlayerActionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["action", getU8Decoder()],
    ["raiseAmount", getU64Decoder()],
  ]);
}

export function getPlayerActionInstructionDataCodec(): FixedSizeCodec<
  PlayerActionInstructionDataArgs,
  PlayerActionInstructionData
> {
  return combineCodec(
    getPlayerActionInstructionDataEncoder(),
    getPlayerActionInstructionDataDecoder(),
  );
}

export type PlayerActionInput<
  TAccountAuthority extends string = string,
  TAccountGame extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  game: Address<TAccountGame>;
  action: PlayerActionInstructionDataArgs["action"];
  raiseAmount: PlayerActionInstructionDataArgs["raiseAmount"];
};

export function getPlayerActionInstruction<
  TAccountAuthority extends string,
  TAccountGame extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: PlayerActionInput<TAccountAuthority, TAccountGame>,
  config?: { programAddress?: TProgramAddress },
): PlayerActionInstruction<TProgramAddress, TAccountAuthority, TAccountGame> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.game),
    ],
    data: getPlayerActionInstructionDataEncoder().encode(
      args as PlayerActionInstructionDataArgs,
    ),
    programAddress,
  } as PlayerActionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame
  >);
}

export type ParsedPlayerActionInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    game: TAccountMetas[1];
  };
  data: PlayerActionInstructionData;
};

export function parsePlayerActionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPlayerActionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), game: getNextAccount() },
    data: getPlayerActionInstructionDataDecoder().decode(instruction.data),
  };
}
