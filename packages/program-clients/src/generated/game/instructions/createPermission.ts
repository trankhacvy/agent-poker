/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { AGENT_POKER_GAME_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getAccountTypeDecoder,
  getAccountTypeEncoder,
  getMemberDecoder,
  getMemberEncoder,
  type AccountType,
  type AccountTypeArgs,
  type Member,
  type MemberArgs,
} from "../types";

export const CREATE_PERMISSION_DISCRIMINATOR = new Uint8Array([
  190, 182, 26, 164, 156, 221, 8, 0,
]);

export function getCreatePermissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_PERMISSION_DISCRIMINATOR,
  );
}

export type CreatePermissionInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountPermissionedAccount extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> =
    "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPermissionedAccount extends string
        ? ReadonlyAccount<TAccountPermissionedAccount>
        : TAccountPermissionedAccount,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreatePermissionInstructionData = {
  discriminator: ReadonlyUint8Array;
  accountType: AccountType;
  members: Option<Array<Member>>;
};

export type CreatePermissionInstructionDataArgs = {
  accountType: AccountTypeArgs;
  members: OptionOrNullable<Array<MemberArgs>>;
};

export function getCreatePermissionInstructionDataEncoder(): Encoder<CreatePermissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["accountType", getAccountTypeEncoder()],
      ["members", getOptionEncoder(getArrayEncoder(getMemberEncoder()))],
    ]),
    (value) => ({ ...value, discriminator: CREATE_PERMISSION_DISCRIMINATOR }),
  );
}

export function getCreatePermissionInstructionDataDecoder(): Decoder<CreatePermissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["accountType", getAccountTypeDecoder()],
    ["members", getOptionDecoder(getArrayDecoder(getMemberDecoder()))],
  ]);
}

export function getCreatePermissionInstructionDataCodec(): Codec<
  CreatePermissionInstructionDataArgs,
  CreatePermissionInstructionData
> {
  return combineCodec(
    getCreatePermissionInstructionDataEncoder(),
    getCreatePermissionInstructionDataDecoder(),
  );
}

export type CreatePermissionInput<
  TAccountPermissionedAccount extends string = string,
  TAccountPermission extends string = string,
  TAccountPayer extends string = string,
  TAccountPermissionProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  permissionedAccount: Address<TAccountPermissionedAccount>;
  permission: Address<TAccountPermission>;
  payer: TransactionSigner<TAccountPayer>;
  permissionProgram?: Address<TAccountPermissionProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  accountType: CreatePermissionInstructionDataArgs["accountType"];
  members: CreatePermissionInstructionDataArgs["members"];
};

export function getCreatePermissionInstruction<
  TAccountPermissionedAccount extends string,
  TAccountPermission extends string,
  TAccountPayer extends string,
  TAccountPermissionProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: CreatePermissionInput<
    TAccountPermissionedAccount,
    TAccountPermission,
    TAccountPayer,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreatePermissionInstruction<
  TProgramAddress,
  TAccountPermissionedAccount,
  TAccountPermission,
  TAccountPayer,
  TAccountPermissionProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    permissionedAccount: {
      value: input.permissionedAccount ?? null,
      isWritable: false,
    },
    permission: { value: input.permission ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.permissionProgram.value) {
    accounts.permissionProgram.value =
      "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1" as Address<"ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.permissionedAccount),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.permissionProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePermissionInstructionDataEncoder().encode(
      args as CreatePermissionInstructionDataArgs,
    ),
    programAddress,
  } as CreatePermissionInstruction<
    TProgramAddress,
    TAccountPermissionedAccount,
    TAccountPermission,
    TAccountPayer,
    TAccountPermissionProgram,
    TAccountSystemProgram
  >);
}

export type ParsedCreatePermissionInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    permissionedAccount: TAccountMetas[0];
    permission: TAccountMetas[1];
    payer: TAccountMetas[2];
    permissionProgram: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreatePermissionInstructionData;
};

export function parseCreatePermissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatePermissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      permissionedAccount: getNextAccount(),
      permission: getNextAccount(),
      payer: getNextAccount(),
      permissionProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatePermissionInstructionDataDecoder().decode(instruction.data),
  };
}
