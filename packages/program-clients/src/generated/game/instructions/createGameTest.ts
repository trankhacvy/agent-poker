/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { AGENT_POKER_GAME_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_GAME_TEST_DISCRIMINATOR = new Uint8Array([
  122, 33, 111, 133, 115, 181, 244, 148,
]);

export function getCreateGameTestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_GAME_TEST_DISCRIMINATOR,
  );
}

export type CreateGameTestInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateGameTestInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  tableId: bigint;
  wagerTier: bigint;
};

export type CreateGameTestInstructionDataArgs = {
  gameId: number | bigint;
  tableId: number | bigint;
  wagerTier: number | bigint;
};

export function getCreateGameTestInstructionDataEncoder(): FixedSizeEncoder<CreateGameTestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["tableId", getU64Encoder()],
      ["wagerTier", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_GAME_TEST_DISCRIMINATOR }),
  );
}

export function getCreateGameTestInstructionDataDecoder(): FixedSizeDecoder<CreateGameTestInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["tableId", getU64Decoder()],
    ["wagerTier", getU64Decoder()],
  ]);
}

export function getCreateGameTestInstructionDataCodec(): FixedSizeCodec<
  CreateGameTestInstructionDataArgs,
  CreateGameTestInstructionData
> {
  return combineCodec(
    getCreateGameTestInstructionDataEncoder(),
    getCreateGameTestInstructionDataDecoder(),
  );
}

export type CreateGameTestAsyncInput<
  TAccountAuthority extends string = string,
  TAccountGame extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  game?: Address<TAccountGame>;
  systemProgram?: Address<TAccountSystemProgram>;
  gameId: CreateGameTestInstructionDataArgs["gameId"];
  tableId: CreateGameTestInstructionDataArgs["tableId"];
  wagerTier: CreateGameTestInstructionDataArgs["wagerTier"];
};

export async function getCreateGameTestInstructionAsync<
  TAccountAuthority extends string,
  TAccountGame extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: CreateGameTestAsyncInput<
    TAccountAuthority,
    TAccountGame,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateGameTestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 107, 101, 114, 95, 103, 97, 109, 101]),
        ),
        getU64Encoder().encode(expectSome(args.gameId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateGameTestInstructionDataEncoder().encode(
      args as CreateGameTestInstructionDataArgs,
    ),
    programAddress,
  } as CreateGameTestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame,
    TAccountSystemProgram
  >);
}

export type CreateGameTestInput<
  TAccountAuthority extends string = string,
  TAccountGame extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  game: Address<TAccountGame>;
  systemProgram?: Address<TAccountSystemProgram>;
  gameId: CreateGameTestInstructionDataArgs["gameId"];
  tableId: CreateGameTestInstructionDataArgs["tableId"];
  wagerTier: CreateGameTestInstructionDataArgs["wagerTier"];
};

export function getCreateGameTestInstruction<
  TAccountAuthority extends string,
  TAccountGame extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: CreateGameTestInput<
    TAccountAuthority,
    TAccountGame,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateGameTestInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountGame,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateGameTestInstructionDataEncoder().encode(
      args as CreateGameTestInstructionDataArgs,
    ),
    programAddress,
  } as CreateGameTestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame,
    TAccountSystemProgram
  >);
}

export type ParsedCreateGameTestInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    game: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateGameTestInstructionData;
};

export function parseCreateGameTestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateGameTestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      game: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateGameTestInstructionDataDecoder().decode(instruction.data),
  };
}
