/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { AGENT_POKER_GAME_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const JOIN_GAME_TEST_DISCRIMINATOR = new Uint8Array([
  78, 253, 179, 121, 142, 227, 33, 193,
]);

export function getJoinGameTestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    JOIN_GAME_TEST_DISCRIMINATOR,
  );
}

export type JoinGameTestInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPlayerHand extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountPlayerHand extends string
        ? WritableAccount<TAccountPlayerHand>
        : TAccountPlayerHand,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type JoinGameTestInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  seatIndex: number;
  player: Address;
};

export type JoinGameTestInstructionDataArgs = {
  gameId: number | bigint;
  seatIndex: number;
  player: Address;
};

export function getJoinGameTestInstructionDataEncoder(): FixedSizeEncoder<JoinGameTestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["seatIndex", getU8Encoder()],
      ["player", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: JOIN_GAME_TEST_DISCRIMINATOR }),
  );
}

export function getJoinGameTestInstructionDataDecoder(): FixedSizeDecoder<JoinGameTestInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["seatIndex", getU8Decoder()],
    ["player", getAddressDecoder()],
  ]);
}

export function getJoinGameTestInstructionDataCodec(): FixedSizeCodec<
  JoinGameTestInstructionDataArgs,
  JoinGameTestInstructionData
> {
  return combineCodec(
    getJoinGameTestInstructionDataEncoder(),
    getJoinGameTestInstructionDataDecoder(),
  );
}

export type JoinGameTestAsyncInput<
  TAccountAuthority extends string = string,
  TAccountGame extends string = string,
  TAccountPlayerHand extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  game?: Address<TAccountGame>;
  playerHand: Address<TAccountPlayerHand>;
  systemProgram?: Address<TAccountSystemProgram>;
  gameId: JoinGameTestInstructionDataArgs["gameId"];
  seatIndex: JoinGameTestInstructionDataArgs["seatIndex"];
  player: JoinGameTestInstructionDataArgs["player"];
};

export async function getJoinGameTestInstructionAsync<
  TAccountAuthority extends string,
  TAccountGame extends string,
  TAccountPlayerHand extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: JoinGameTestAsyncInput<
    TAccountAuthority,
    TAccountGame,
    TAccountPlayerHand,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  JoinGameTestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame,
    TAccountPlayerHand,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    playerHand: { value: input.playerHand ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 107, 101, 114, 95, 103, 97, 109, 101]),
        ),
        getU64Encoder().encode(expectSome(args.gameId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.playerHand),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getJoinGameTestInstructionDataEncoder().encode(
      args as JoinGameTestInstructionDataArgs,
    ),
    programAddress,
  } as JoinGameTestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame,
    TAccountPlayerHand,
    TAccountSystemProgram
  >);
}

export type JoinGameTestInput<
  TAccountAuthority extends string = string,
  TAccountGame extends string = string,
  TAccountPlayerHand extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  game: Address<TAccountGame>;
  playerHand: Address<TAccountPlayerHand>;
  systemProgram?: Address<TAccountSystemProgram>;
  gameId: JoinGameTestInstructionDataArgs["gameId"];
  seatIndex: JoinGameTestInstructionDataArgs["seatIndex"];
  player: JoinGameTestInstructionDataArgs["player"];
};

export function getJoinGameTestInstruction<
  TAccountAuthority extends string,
  TAccountGame extends string,
  TAccountPlayerHand extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: JoinGameTestInput<
    TAccountAuthority,
    TAccountGame,
    TAccountPlayerHand,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): JoinGameTestInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountGame,
  TAccountPlayerHand,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    playerHand: { value: input.playerHand ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.playerHand),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getJoinGameTestInstructionDataEncoder().encode(
      args as JoinGameTestInstructionDataArgs,
    ),
    programAddress,
  } as JoinGameTestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame,
    TAccountPlayerHand,
    TAccountSystemProgram
  >);
}

export type ParsedJoinGameTestInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    game: TAccountMetas[1];
    playerHand: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: JoinGameTestInstructionData;
};

export function parseJoinGameTestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedJoinGameTestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      game: getNextAccount(),
      playerHand: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getJoinGameTestInstructionDataDecoder().decode(instruction.data),
  };
}
