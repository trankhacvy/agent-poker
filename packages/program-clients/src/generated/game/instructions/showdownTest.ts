/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AGENT_POKER_GAME_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SHOWDOWN_TEST_DISCRIMINATOR = new Uint8Array([
  53, 72, 132, 171, 97, 89, 75, 159,
]);

export function getShowdownTestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SHOWDOWN_TEST_DISCRIMINATOR,
  );
}

export type ShowdownTestInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      ...TRemainingAccounts,
    ]
  >;

export type ShowdownTestInstructionData = { discriminator: ReadonlyUint8Array };

export type ShowdownTestInstructionDataArgs = {};

export function getShowdownTestInstructionDataEncoder(): FixedSizeEncoder<ShowdownTestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SHOWDOWN_TEST_DISCRIMINATOR }),
  );
}

export function getShowdownTestInstructionDataDecoder(): FixedSizeDecoder<ShowdownTestInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getShowdownTestInstructionDataCodec(): FixedSizeCodec<
  ShowdownTestInstructionDataArgs,
  ShowdownTestInstructionData
> {
  return combineCodec(
    getShowdownTestInstructionDataEncoder(),
    getShowdownTestInstructionDataDecoder(),
  );
}

export type ShowdownTestInput<
  TAccountAuthority extends string = string,
  TAccountGame extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  game: Address<TAccountGame>;
};

export function getShowdownTestInstruction<
  TAccountAuthority extends string,
  TAccountGame extends string,
  TProgramAddress extends Address = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
>(
  input: ShowdownTestInput<TAccountAuthority, TAccountGame>,
  config?: { programAddress?: TProgramAddress },
): ShowdownTestInstruction<TProgramAddress, TAccountAuthority, TAccountGame> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENT_POKER_GAME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.game),
    ],
    data: getShowdownTestInstructionDataEncoder().encode({}),
    programAddress,
  } as ShowdownTestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGame
  >);
}

export type ParsedShowdownTestInstruction<
  TProgram extends string = typeof AGENT_POKER_GAME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    game: TAccountMetas[1];
  };
  data: ShowdownTestInstructionData;
};

export function parseShowdownTestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedShowdownTestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), game: getNextAccount() },
    data: getShowdownTestInstructionDataDecoder().decode(instruction.data),
  };
}
