/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PLAYER_HAND_DISCRIMINATOR = new Uint8Array([
  16, 240, 39, 61, 10, 73, 250, 160,
]);

export function getPlayerHandDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PLAYER_HAND_DISCRIMINATOR);
}

export type PlayerHand = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  player: Address;
  hand: ReadonlyUint8Array;
  bump: number;
};

export type PlayerHandArgs = {
  gameId: number | bigint;
  player: Address;
  hand: ReadonlyUint8Array;
  bump: number;
};

/** Gets the encoder for {@link PlayerHandArgs} account data. */
export function getPlayerHandEncoder(): FixedSizeEncoder<PlayerHandArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["player", getAddressEncoder()],
      ["hand", fixEncoderSize(getBytesEncoder(), 2)],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PLAYER_HAND_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link PlayerHand} account data. */
export function getPlayerHandDecoder(): FixedSizeDecoder<PlayerHand> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["player", getAddressDecoder()],
    ["hand", fixDecoderSize(getBytesDecoder(), 2)],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link PlayerHand} account data. */
export function getPlayerHandCodec(): FixedSizeCodec<
  PlayerHandArgs,
  PlayerHand
> {
  return combineCodec(getPlayerHandEncoder(), getPlayerHandDecoder());
}

export function decodePlayerHand<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PlayerHand, TAddress>;
export function decodePlayerHand<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PlayerHand, TAddress>;
export function decodePlayerHand<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<PlayerHand, TAddress> | MaybeAccount<PlayerHand, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPlayerHandDecoder(),
  );
}

export async function fetchPlayerHand<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PlayerHand, TAddress>> {
  const maybeAccount = await fetchMaybePlayerHand(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePlayerHand<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PlayerHand, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePlayerHand(maybeAccount);
}

export async function fetchAllPlayerHand(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PlayerHand>[]> {
  const maybeAccounts = await fetchAllMaybePlayerHand(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePlayerHand(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PlayerHand>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePlayerHand(maybeAccount));
}

export function getPlayerHandSize(): number {
  return 51;
}
