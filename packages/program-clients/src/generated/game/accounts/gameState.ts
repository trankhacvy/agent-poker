/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getGamePhaseDecoder,
  getGamePhaseEncoder,
  type GamePhase,
  type GamePhaseArgs,
} from "../types";

export const GAME_STATE_DISCRIMINATOR = new Uint8Array([
  144, 94, 208, 172, 248, 99, 134, 120,
]);

export function getGameStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GAME_STATE_DISCRIMINATOR);
}

export type GameState = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  tableId: bigint;
  playerCount: number;
  wagerTier: bigint;
  phase: GamePhase;
  pot: bigint;
  currentBet: bigint;
  dealerIndex: number;
  currentPlayer: number;
  lastRaiser: number;
  /** Ordered deck after VRF shuffle — community cards live at indices 12-16. */
  deck: ReadonlyUint8Array;
  players: Array<Address>;
  playerStatus: ReadonlyUint8Array;
  playerBets: Array<bigint>;
  communityCards: ReadonlyUint8Array;
  communityCount: number;
  winnerIndex: number;
  authority: Address;
  bump: number;
  createdAt: bigint;
  lastActionAt: bigint;
};

export type GameStateArgs = {
  gameId: number | bigint;
  tableId: number | bigint;
  playerCount: number;
  wagerTier: number | bigint;
  phase: GamePhaseArgs;
  pot: number | bigint;
  currentBet: number | bigint;
  dealerIndex: number;
  currentPlayer: number;
  lastRaiser: number;
  /** Ordered deck after VRF shuffle — community cards live at indices 12-16. */
  deck: ReadonlyUint8Array;
  players: Array<Address>;
  playerStatus: ReadonlyUint8Array;
  playerBets: Array<number | bigint>;
  communityCards: ReadonlyUint8Array;
  communityCount: number;
  winnerIndex: number;
  authority: Address;
  bump: number;
  createdAt: number | bigint;
  lastActionAt: number | bigint;
};

/** Gets the encoder for {@link GameStateArgs} account data. */
export function getGameStateEncoder(): FixedSizeEncoder<GameStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["tableId", getU64Encoder()],
      ["playerCount", getU8Encoder()],
      ["wagerTier", getU64Encoder()],
      ["phase", getGamePhaseEncoder()],
      ["pot", getU64Encoder()],
      ["currentBet", getU64Encoder()],
      ["dealerIndex", getU8Encoder()],
      ["currentPlayer", getU8Encoder()],
      ["lastRaiser", getU8Encoder()],
      ["deck", fixEncoderSize(getBytesEncoder(), 52)],
      ["players", getArrayEncoder(getAddressEncoder(), { size: 6 })],
      ["playerStatus", fixEncoderSize(getBytesEncoder(), 6)],
      ["playerBets", getArrayEncoder(getU64Encoder(), { size: 6 })],
      ["communityCards", fixEncoderSize(getBytesEncoder(), 5)],
      ["communityCount", getU8Encoder()],
      ["winnerIndex", getU8Encoder()],
      ["authority", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["createdAt", getI64Encoder()],
      ["lastActionAt", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: GAME_STATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link GameState} account data. */
export function getGameStateDecoder(): FixedSizeDecoder<GameState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["tableId", getU64Decoder()],
    ["playerCount", getU8Decoder()],
    ["wagerTier", getU64Decoder()],
    ["phase", getGamePhaseDecoder()],
    ["pot", getU64Decoder()],
    ["currentBet", getU64Decoder()],
    ["dealerIndex", getU8Decoder()],
    ["currentPlayer", getU8Decoder()],
    ["lastRaiser", getU8Decoder()],
    ["deck", fixDecoderSize(getBytesDecoder(), 52)],
    ["players", getArrayDecoder(getAddressDecoder(), { size: 6 })],
    ["playerStatus", fixDecoderSize(getBytesDecoder(), 6)],
    ["playerBets", getArrayDecoder(getU64Decoder(), { size: 6 })],
    ["communityCards", fixDecoderSize(getBytesDecoder(), 5)],
    ["communityCount", getU8Decoder()],
    ["winnerIndex", getU8Decoder()],
    ["authority", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["createdAt", getI64Decoder()],
    ["lastActionAt", getI64Decoder()],
  ]);
}

/** Gets the codec for {@link GameState} account data. */
export function getGameStateCodec(): FixedSizeCodec<GameStateArgs, GameState> {
  return combineCodec(getGameStateEncoder(), getGameStateDecoder());
}

export function decodeGameState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GameState, TAddress>;
export function decodeGameState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GameState, TAddress>;
export function decodeGameState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GameState, TAddress> | MaybeAccount<GameState, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGameStateDecoder(),
  );
}

export async function fetchGameState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GameState, TAddress>> {
  const maybeAccount = await fetchMaybeGameState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGameState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GameState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGameState(maybeAccount);
}

export async function fetchAllGameState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GameState>[]> {
  const maybeAccounts = await fetchAllMaybeGameState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGameState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GameState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGameState(maybeAccount));
}

export function getGameStateSize(): number {
  return 407;
}
